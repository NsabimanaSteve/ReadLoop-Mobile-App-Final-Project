import 'package:flutter/material.dart';

void main() {
  runApp(
    MaterialApp(
      title: 'ReadLoop',
      home: Scaffold(
        body: Center(
          child: Text('ReadLoop App is Running!', style: TextStyle(fontSize: 24)),
        ),
      ),
    ),
  );
}
